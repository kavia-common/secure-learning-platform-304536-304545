# Express backend configuration
PORT=3001
HOST=0.0.0.0
NODE_ENV=development

# MongoDB connection (env-first; server falls back to local default if missing)
MONGO_URI=mongodb://127.0.0.1:27017/secure_learning_platform

# JWT secret (intentionally weak default exists in code; set this to override)
JWT_SECRET=dev-secret-change-me
